<%= form_for(@arec) do |f| %>
  <% if @arec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arec.errors.count, "error") %> prohibited this arec from being saved:</h2>

      <ul>
      <% @arec.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
